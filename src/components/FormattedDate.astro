---
interface Props {
  date: Date;
}

const { date } = Astro.props;

const dateIso = date.toISOString();
const dateHuman = date.toLocaleDateString('en-GB', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
---

<time datetime={dateIso}>{dateHuman}</time>
